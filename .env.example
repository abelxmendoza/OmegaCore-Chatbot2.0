# ============================================
# OMEGA-CORE A.I. - Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to Git!

# ============================================
# AUTHENTICATION (Required)
# ============================================

# Secret key for NextAuth encryption/signing
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-random-secret-here-generate-with-openssl

# Legacy NextAuth secret (can be same as AUTH_SECRET)
# NextAuth v5 prefers AUTH_SECRET, but also supports NEXTAUTH_SECRET
NEXTAUTH_SECRET=your-random-secret-here-generate-with-openssl

# Base URL for authentication (local development)
# For Vercel production: https://your-app.vercel.app
NEXTAUTH_URL=http://localhost:3000

# Alternative auth URL (optional)
# For Vercel production: https://your-app.vercel.app/
AUTH_URL=http://localhost:3000/

# Trust host header (optional - code handles this with trustHost: true)
# Set to 'true' for Vercel deployments
AUTH_TRUST_HOST=true

# ============================================
# DATABASE (Required)
# ============================================

# PostgreSQL connection string (Supabase or other PostgreSQL database)
# Format: postgresql://user:password@host:port/database
# Example: postgresql://postgres:password@db.xxx.supabase.co:5432/postgres
POSTGRES_URL=postgresql://user:password@host:port/database

# Legacy database URL (deprecated - use POSTGRES_URL instead)
# DATABASE_URL=postgresql://user:password@host:port/database

# ============================================
# LLM API KEYS (At least one required)
# ============================================

# OpenAI API Key (Required for GPT-4 models)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# xAI API Key (Optional - for Grok models)
# Get from: https://x.ai/api
# Recommended for security research
XAI_API_KEY=your-xai-api-key-here

# Anthropic API Key (Optional - for Claude models)
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# ============================================
# FILE STORAGE (Optional)
# ============================================

# Vercel Blob Storage token (for file uploads)
# Get from: https://vercel.com/dashboard/stores
BLOB_READ_WRITE_TOKEN=vercel_blob_your-token-here

# ============================================
# APP CONFIGURATION (Optional)
# ============================================

# Public app name (displayed in UI)
NEXT_PUBLIC_APP_NAME=Omega-Core

# Default AI model provider
# Options: openai, xai, anthropic
AI_MODEL_PROVIDER=openai

# ============================================
# VERCEL DEPLOYMENT NOTES
# ============================================
# When deploying to Vercel:
# 1. Copy all variables to Vercel Dashboard → Settings → Environment Variables
# 2. Change NEXTAUTH_URL to: https://your-app.vercel.app
# 3. Change AUTH_URL to: https://your-app.vercel.app/
# 4. Set for: Production, Preview, Development
# 5. Delete NEXTAUTH_URL_INTERNAL (not needed)
# 6. Delete DATABASE_URL (use POSTGRES_URL instead)
# 7. Delete NEXTAUTH_SECRET (use AUTH_SECRET instead, or keep both same)
#
# See VERCEL_ENV_COPY_PASTE.md for detailed instructions

# ============================================
# LOCAL DEVELOPMENT ONLY (Do not set in Vercel)
# ============================================

# Internal NextAuth URL (not needed - trustHost handles this)
# NEXTAUTH_URL_INTERNAL=http://localhost:3000
